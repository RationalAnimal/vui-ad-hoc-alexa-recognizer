{
  "description": "Simplest possible domain - no intent sequences, excluded intents, single recognizer passed in as path string, not using states, not using responses, not referencing other domains",
  "recognizers": [
    {
      "key": "theUsual",
      "recognizer": {}
    }
  ],
  "states": [
    {
      "matchCriteria": "default",
      "matchSpecs": [
        {
          "recognizer": "theUsual"
        }
      ]
    }
  ]
}